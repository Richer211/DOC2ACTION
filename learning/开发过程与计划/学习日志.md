# 学习日志

> 目标：边开发边学习，把“会做”沉淀成“会讲”。

---

## 日志模板（复制使用）

### 日期：YYYY-MM-DD

### 今日开发内容

- 

### 今日学习点（技术/产品/工程）

- 

### 遇到的问题

- 

### 解决方案

- 

### 为什么这样做（取舍）

- 

### 可复用经验

- 

### 明天计划

- 

---

## 实际记录

### 日期：2026-03-22

### 今日开发内容

- 明确 Doc2Action 的 PRD、MVP 与开发拆分
- 建立学习与面试准备文档体系

### 今日学习点（技术/产品/工程）

- 求职项目需要同时覆盖“实现能力”和“表达能力”
- MVP 边界明确比功能堆叠更重要

### 遇到的问题

- 如何在开发初期就积累可面试素材，而不是最后补

### 解决方案

- 新建统一文档目录，按“日志 + 面试题库 + 阶段复盘”并行维护

### 为什么这样做（取舍）

- 增加少量文档成本，换来更强的项目叙事与面试稳定性

### 可复用经验

- 从 Day 1 开始记录“问题 -> 决策 -> 结果”，比事后回忆更准确

### 明天计划

- 开始 Phase 2：实现 /analyze 最小链路（输入 -> 结构化 JSON 输出）

---

### 日期：2026-03-22（追加记录）

### 今日开发内容

- 完成 Phase 1 项目骨架：`frontend` + `backend`
- 后端实现 `GET /health` 接口与 CORS
- 前端首页接入后端健康检查请求
- 新增根目录 `README.md` 和环境说明

### 今日学习点（技术/产品/工程）

- 环境兼容性本身就是工程能力的一部分（Node 版本会直接影响脚手架初始化）
- 在求职项目中，清晰的运行说明和目录结构同样是加分项

### 遇到的问题

- 本机 Node 版本过低，无法直接执行 Next.js 脚手架命令

### 解决方案

- 先手工搭建 Next.js 标准骨架文件，保证项目结构和开发方向不阻塞
- 在 README 中明确 Node 升级要求，确保后续可一键安装依赖并运行

### 为什么这样做（取舍）

- 优先保障 Phase 1 目标可落地，再处理本地环境升级，避免开发节奏中断

### 可复用经验

- 遇到环境阻塞时，先让工程结构前进，再回补环境依赖

### 明天计划

- 升级 Node 到 >=18.17 后跑通前端安装和启动验证
- 进入 Phase 2：`POST /analyze` 接口最小版本

---

### 日期：2026-03-22（Phase 2）

### 今日开发内容

- 使用 `nvm` 切换 Node 到 `v22.16.0`，完成前端依赖安装
- 后端新增 `POST /analyze` 接口，支持：
  - 文本清洗
  - chunk 切分与编号
  - 结构化输出（summary / action_items / risks / open_questions）
- 接入 OpenAI 调用能力，并在失败时自动降级为本地规则提取
- 修复前端 ESLint 配置，确保 `npm run lint` 通过

### 今日学习点（技术/产品/工程）

- AI 项目要有“降级策略”，避免 API key/网络问题阻塞主流程
- MVP 阶段要优先保证接口稳定可测，再逐步增强模型效果

### 遇到的问题

- 本机环境中存在无效 OpenAI Key，导致接口首次调用报 401
- Next.js ESLint 扩展配置不兼容当前版本

### 解决方案

- 在 LLM 调用处增加异常兜底，失败时走规则提取
- 调整前端 ESLint 扩展为 `next/core-web-vitals`

### 为什么这样做（取舍）

- 牺牲一点“全依赖模型”的纯度，换来可持续开发和可演示稳定性

### 可复用经验

- Phase 早期先做“可运行 + 可验证 + 可降级”的基础版本，后续再优化精度

### 明天计划

- 进入 Phase 3：前端输入区 + 结果展示区，打通 `/analyze` 调用

---

### 日期：2026-03-22（Phase 3）

### 今日开发内容

- 前端首页升级为工作台页面，支持文本输入和文件上传（`.txt/.md`）
- 新增文档类型选择（general / meeting_notes / prd / email_thread / sop）
- 打通前端到后端 `POST /analyze` 调用
- 完成四类结果展示（Summary / Action Items / Risks / Open Questions）
- 增加结果元信息展示（document_type / chunk_count / used_llm）

### 今日学习点（技术/产品/工程）

- 前端状态设计要按“输入态、加载态、成功态、失败态”拆分，页面才稳定
- 先做信息架构清晰的结果展示，再做高级交互（编辑、引用详情）更高效

### 遇到的问题

- 需要同时兼顾“开发调试可读性”和“面试演示可理解性”

### 解决方案

- 保留明确模块化区块和基础元信息，让演示时能快速说明系统行为

### 为什么这样做（取舍）

- 当前先不做复杂样式和可编辑细节，优先保证主链路稳、讲解清楚

### 可复用经验

- 求职项目里，界面清楚地呈现“输入 -> 输出结构 -> 依据元数据”就是强证据

### 明天计划

- 进入 Phase 4：实现按条目查看引用 chunk 原文（结果可追溯）

---

### 日期：2026-03-22（Phase 4）

### 今日开发内容

- 前端结果列表支持点击条目（Action / Risk / Open Question）
- 新增“引用依据”侧栏，展示条目类型、source ids、对应 chunk 原文
- 增加两类提示：
  - 条目无 source id 时提示人工补充确认
  - source id 无法映射 chunk 时提示后端映射异常

### 今日学习点（技术/产品/工程）

- “可追溯”不只是返回字段，还需要清晰的交互入口和异常提示闭环
- 面试演示中，用点击交互展示 evidence，会比静态文本更有说服力

### 遇到的问题

- 要保证引用查看功能可用，同时不破坏现有结果展示阅读体验

### 解决方案

- 采用左右分区布局：左侧结果，右侧证据面板，互不干扰

### 为什么这样做（取舍）

- 先用最直观的点击交互完成追溯能力，再考虑高阶筛选和高亮

### 可复用经验

- 在 AI 产品里，结果可信度建设应包含“正常路径 + 异常路径”的完整设计

### 明天计划

- 进入 Phase 5：支持结果编辑与 Markdown 导出

---

### 日期：2026-03-22（Phase 5）

### 今日开发内容

- 完成 `summary` 可编辑
- 完成 `action items` 新增 / 修改 / 删除
- 保留引用依据交互，支持在编辑场景下继续查看 source chunk
- 新增 Markdown 导出按钮，导出当前编辑后的结果

### 今日学习点（技术/产品/工程）

- 求职项目中“human-in-the-loop”要有真实交互，不只是口头描述
- 导出能力是闭环落地的重要证据，能直接体现项目的可用性

### 遇到的问题

- 在编辑后如何保持“查看引用依据”能力不丢失

### 解决方案

- 采用独立 `editableResult` 状态，编辑与引用都基于同一份数据源

### 为什么这样做（取舍）

- 增加状态管理复杂度，但换来更稳定的一致性和更好的演示效果

### 可复用经验

- 对 AI 结果做后处理时，优先建立“可编辑状态层”，避免读写混乱

### 明天计划

- 进入 Phase 6：准备 3-5 组样本，验证稳定性并打磨 demo 脚本

---

### 日期：2026-03-22（Phase 6）

### 今日开发内容

- 新增 5 份测试样本（meeting / prd / email / sop / mixed）
- 新增批量检查脚本 `scripts/run_sample_checks.py`
- 生成样本检查报告与明细结果到 `learning/报告与演示材料/demo/`
- 新增 `learning/报告与演示材料/phase6测试记录.md` 与 `learning/报告与演示材料/demo/3-5分钟演示脚本.md`

### 今日学习点（技术/产品/工程）

- Demo 准备不只是“能跑”，还要有稳定样本和可复现脚本
- 批量样本验证能快速暴露规则提取的边界和误判类型

### 遇到的问题

- 脚本首次运行时出现模块导入路径错误

### 解决方案

- 在脚本中显式注入 `backend` 路径，保证跨目录运行一致性

### 为什么这样做（取舍）

- 牺牲一点脚本简洁性，换来更稳定的可执行性和团队可复用性

### 可复用经验

- 对作品项目来说，“样本 + 自动检查 + 演示讲稿”是高价值交付件

### 明天计划

- 根据样本报告优化规则提取，减少 risk/action 的误判

---

### 日期：2026-03-22（Phase B 启动：数据与评估）

### 今日开发内容

- 新建 `ml/data/schema.json`，定义结构化标注规范
- 新建 `ml/data/train.sample.jsonl`，提供首批训练样本
- 新建 `ml/eval/evaluate.py`，实现 baseline 离线评估
- 新建架构报告：`learning/报告与演示材料/reports/架构与算法设计报告-v0.1.md`
- 新建资源清单：`learning/开发过程与计划/资源需求清单.md`

### 今日学习点（技术/产品/工程）

- 算法能力要落在“数据 + 指标 + 实验”三件套上
- 项目架构报告是面试深挖环节的重要证据，而不只是附加文档

### 遇到的问题

- 仅有 MVP 功能无法充分证明算法岗能力，需要补齐训练与评估路径

### 解决方案

- 从 Phase B 开始优先建设数据规范与评估体系，为后续 LoRA 训练铺路

### 为什么这样做（取舍）

- 先做评估基线再做训练，能避免“训练了但不知道是否变好”

### 可复用经验

- 先固化数据协议与指标定义，后续多模型对比才有可比性

### 明天计划

- 运行 baseline 评估并输出第一版指标报告
- 准备首批 20-30 条脱敏真实数据用于标注

---

### 日期：2026-03-22（Phase B 评估进展）

### 今日开发内容

- 完成 baseline 评估运行：`ml/eval/evaluate.py`
- 输出报告：`learning/报告与演示材料/reports/baseline-eval.md` 与 `baseline-eval.json`
- 将评估匹配策略升级为中文友好近似匹配（字符 n-gram + 包含关系）

### 初始指标快照

- action_f1: 0.15
- risk_f1: 0.1667
- question_f1: 1.0
- summary_jaccard: 0.171
- citation_hit_rate: 1.0

### 学习结论

- 当前规则对 open questions 表现较好，但 action/risk 仍有明显优化空间
- 这组 baseline 指标将作为后续 LoRA 与 RAG 对比基线

### 下一步

- 先补 20-30 条真实脱敏数据并按 schema 标注
- 再做规则迭代前后对比，确保 action/risk 指标可量化提升

---

### 日期：2026-03-22（Enron 数据接入）

### 今日开发内容

- 确认 Enron 数据已放置到 `ml/data/external/enron/extracted/maildir`
- 新增 Enron 数据处理脚本：
  - `scripts/sample_enron.py`（抽样清洗）
  - `scripts/build_seed_from_enron.py`（弱标注转换）
  - `scripts/process_enron_pipeline.py`（一键处理）
  - `scripts/anonymize_seed_jsonl.py`（脱敏输出）
- 已生成首批 30 条弱标注 seed，并生成脱敏版本
- 新增文档：`learning/数据与标注规范/Enron数据处理指南.md`

### 今日学习点（技术/产品/工程）

- 公开语料接入要与主应用解耦，放在数据准备脚本层
- 弱标注数据必须经过人工修订，不能直接作为高质量训练集

### 遇到的问题

- Enron 原始邮件噪声高（转发链、回复头、编码问题）

### 解决方案

- 先做正文清洗与长度过滤，再做结构化弱标注，最后统一脱敏

### 为什么这样做（取舍）

- 先保留规模与覆盖，再通过抽检提升质量，平衡效率和准确度

### 明天计划

- 从 Enron 弱标注中抽 50 条做人工精修，作为第一批高质量训练集

---

### 日期：2026-03-22（AMI 数据接入）

### 今日开发内容

- 确认 AMI 数据按规范放置到 `ml/data/external/ami/raw` 与 `extracted`
- 新增 AMI 处理脚本：
  - `scripts/sample_ami.py`
  - `scripts/build_seed_from_ami.py`
  - `scripts/process_ami_pipeline.py`
- 跑通 30 条样本抽样与弱标注转换
- 新增文档：`learning/数据与标注规范/AMI数据处理指南.md`

### 今日学习点（技术/产品/工程）

- 会议转录 XML 语料可通过统一解析层复用到项目标注格式
- 公共语料适合做训练种子，但必须通过人工修订提升质量

### 遇到的问题

- AMI 转录口语噪声高，自动 action 提取偏长

### 解决方案

- 先保证规模覆盖，后续通过人工精修和规则约束缩短 action 粒度

### 明天计划

- 从 AMI seed 抽取 50 条进行人工修订，形成 `ami.curated.v1`

---

### 日期：2026-03-23（Jira 数据链路打通）

### 今日开发内容

- 安装 MongoDB 工具链：`mongodb-database-tools`、`mongosh`、`mongodb-community`
- 启动本地 MongoDB 服务并恢复 Jira dump（`mongodump-JiraReposAnon.archive`）
- 新增 Jira 处理脚本：
  - `scripts/sample_jira.py`
  - `scripts/build_seed_from_jira.py`
  - `scripts/process_jira_pipeline.py`
- 跑通 Jira 全流程并产出：
  - `ml/data/external/jira/processed/jira.sample.jsonl`（400 条）
  - `ml/data/external/jira/processed/jira.seed.auto.jsonl`（400 条）
- 新增文档：`learning/数据与标注规范/Jira数据处理指南.md`

### 今日学习点（技术/产品/工程）

- 大体量公开数据集通常需要独立恢复层（数据库）再进入 NLP 训练流水线
- 先建立稳定抽样与弱标注，再进入人工精修，效率更高

### 遇到的问题

- Jira 数据为 MongoDB dump，不能直接按文本文件读取
- 恢复阶段有跨版本告警（7.x dump -> 8.x restore）

### 解决方案

- 在本地安装并启动 MongoDB，使用 `mongorestore --gzip --archive` 恢复
- 恢复后通过 `pymongo` 按统一字段抽样，再用现有 `analyze` 做弱标注

### 为什么这样做（取舍）

- 增加了环境复杂度，但换来可复现的数据处理链路和可扩展的数据工程能力

### 明天计划

- 将 Jira seed 合并到 `curation.batch`
- 对 Jira 样本做 30-50 条人工精修，关注 action/risk 质量

---

### 日期：2026-03-23（主线推进：基线对比 + Jira Top30）

### 今日开发内容

- 跑通低成本 LLM 与规则基线对比：`learning/报告与演示材料/reports/baseline-compare.md`
- 结果：`action_f1` 与 `risk_f1` 相比规则基线显著提升
- 新增 Jira 高优先精修脚本：`scripts/prepare_jira_priority_curation.py`
- 产出 Jira 人工精修清单：
  - `ml/data/curation/jira.curate.top30.v1.jsonl`
  - `learning/报告与演示材料/reports/jira-curation-top30.md`

### 今日学习点（技术/产品/工程）

- 先做低成本模型对比可以快速验证“是否值得训练闭环”
- 人工精修应优先选择“高噪声、高收益”样本，而非随机抽样

### 遇到的问题

- 环境变量存在旧 key 覆盖 `.env` 的问题，导致 LLM 未生效

### 解决方案

- 修改 dotenv 加载为 `override=True`，确保 `.env` 显式配置优先

### 明天计划

- 完成 Jira Top30 首轮人工精修
- 基于精修样本重跑评估，验证 `action/risk` 改善幅度

---

### 日期：2026-03-23（主线推进：Top30 草案与批次合并）

### 今日开发内容

- 新增 LLM 辅助草案脚本：`scripts/refine_jira_top30_with_llm.py`
- 新增覆盖合并脚本：`scripts/merge_curation_overrides.py`
- 新增前后质量对比脚本：`scripts/compare_curation_quality.py`
- 产出草案精修集：`ml/data/curated/jira.curated.top30.draft.v1.jsonl`
- 合并生成新批次：`ml/data/curation/curation.batch.v2.jsonl`
- 输出报告：
  - `learning/报告与演示材料/reports/jira-curation-draft-summary.md`
  - `learning/报告与演示材料/reports/jira-curation-quality-compare.md`
  - `learning/报告与演示材料/reports/curation-validation.v2.md`

### 今日学习点（技术/产品/工程）

- 弱标注到高质量标注应采用“模型草案 + 人工复核”的混合流程
- 质量提升应优先看可解释指标（metadata 比例、长 action 比例、空风险比例）

### 关键结果

- `metadata_summary_rate`: 1.0 -> 0.0
- `long_action_rate`: 1.0 -> 0.0
- `empty_risk_rate`: 1.0 -> 0.2667
- `empty_question_rate`: 1.0 -> 0.3333

### 明天计划

- 人工复核 `jira.curated.top30.draft.v1.jsonl`
- 将人工确认版作为 `gold` 子集，进入训练/评估闭环
